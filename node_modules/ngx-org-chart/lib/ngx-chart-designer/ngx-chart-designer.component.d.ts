import { EventEmitter } from '@angular/core';
import { INode } from '../node';
export declare class NgxChartDesignerComponent {
    node: INode;
    hasParent: boolean;
    direction: 'vertical' | 'horizontal';
    itemClick: EventEmitter<INode>;
    readonly hostClass: "" | "column";
}
